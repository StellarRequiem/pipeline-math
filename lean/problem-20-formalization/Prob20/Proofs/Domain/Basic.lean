import Prob20.Theorems
import Prob20.Proofs.Domain.ResidueField
import Prob20.Proofs.Domain.FractionField

/-!
# Stage 1 — the counterexample domain (`Proofs/Domain/`)

Placeholder module for the Stage 1 support proofs (SKETCH *Counterexample
Domain*): `Dom` is an integral domain, the algebra tower `Dom ⊆ Tsub ⊆ Kt`,
`D_residue_field` (`D/𝔪 ≅ 𝔽₂`), and `D_fraction_field` (`Frac(D) = 𝔽₂(t)`).

The frozen statements live in `Prob20/Theorems.lean`; their proofs are added to
this directory during the proving phase and re-exposed in `Prob20/Solution.lean`.
-/

namespace Prob20.Proofs.Domain

end Prob20.Proofs.Domain
