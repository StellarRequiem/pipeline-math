import Prob20.Theorems
import Prob20.Proofs.Injective.Kernel
import Prob20.Proofs.Injective.Amplify

/-!
# Stage 3 — failure of injectivity (`Proofs/Injective/`)

Placeholder module for the Stage 3 support proofs (SKETCH *Failure of
Injectivity*): the explicit nonzero kernel element `τ = (tp)⊗p − p⊗(tp)`
(`theta2_kernel`) and the `∀ n ≥ 2` lift `thetaN_not_injective`.

The frozen statements live in `Prob20/Theorems.lean`; their proofs are added to
this directory during the proving phase and re-exposed in `Prob20/Solution.lean`.
-/

namespace Prob20.Proofs.Injective

end Prob20.Proofs.Injective
