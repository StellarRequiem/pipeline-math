import Prob20.Theorems
import Prob20.Proofs.KeyObs.KeyMembership
import Prob20.Proofs.KeyObs.LinIndep

/-!
# Stage 2 — the Key Observation (`Proofs/KeyObs/`)

Placeholder module for the Stage 2 support proofs (SKETCH *Key Observation*):
`key_membership` (`p, tp, (t+1)p ∈ Int(D)`) and `p_tp_linindep` (the `𝔽₂`-linear
independence of `p̄, t̄p̄` in `Int(D)/𝔪·Int(D)`, a discrete-valuation computation).

The frozen statements live in `Prob20/Theorems.lean`; their proofs are added to
this directory during the proving phase and re-exposed in `Prob20/Solution.lean`.
-/

namespace Prob20.Proofs.KeyObs

end Prob20.Proofs.KeyObs
