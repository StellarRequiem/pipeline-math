import Prob20.Theorems
import Prob20.Proofs.Surjective.GMem
import Prob20.Proofs.Surjective.Place1
import Prob20.Proofs.Surjective.Amplify
import Prob20.Proofs.Surjective.NotSurjective

/-!
# Stage 4 — failure of surjectivity (`Proofs/Surjective/`)

Placeholder module for the Stage 4 support proofs (SKETCH *Failure of
Surjectivity*): `g_mem` (`g ∈ Int(D)`), `theta2_missing` (`P = g(XY) ∈ Int(D²)`
but `P ∉ im θ₂`, via the finite-difference + valuation argument), and the
`∀ n ≥ 2` lift `thetaN_not_surjective`.

The frozen statements live in `Prob20/Theorems.lean`; their proofs are added to
this directory during the proving phase and re-exposed in `Prob20/Solution.lean`.
-/

namespace Prob20.Proofs.Surjective

end Prob20.Proofs.Surjective
